<Window x:Class="SkillExam.App.Controls.ErrorSummaryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="题库报告" Width="760" Height="560" Style="{StaticResource DialogWindowStyle}"
        WindowStartupLocation="CenterOwner" SizeToContent="Manual" ResizeMode="CanResizeWithGrip">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
        <TextBlock Text="{Binding DialogTitle}" Style="{StaticResource SectionTitleTextStyle}" />
        <TextBlock Grid.Row="1" Text="{Binding Summary}" Style="{StaticResource SecondaryTextStyle}" Margin="0,8,0,14" />
        <DataGrid Grid.Row="2" ItemsSource="{Binding Issues}" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal" RowHeaderWidth="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Sheet" Binding="{Binding Sheet}" Width="1.4*" />
                <DataGridTextColumn Header="行" Binding="{Binding RowNumber}" Width="60" />
                <DataGridTextColumn Header="字段" Binding="{Binding Field}" Width="1.1*" />
                <DataGridTextColumn Header="说明" Binding="{Binding Message}" Width="2.5*" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Row="3" Content="关闭" Click="Close_OnClick" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right" Margin="0,18,0,0" />
    </Grid>
</Window>
