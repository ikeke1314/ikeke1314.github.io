<Window x:Class="SkillExam.App.Views.SheetSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择题库 Sheet" Width="540" Height="520" Style="{StaticResource DialogWindowStyle}"
        WindowStartupLocation="CenterOwner" SizeToContent="Manual" ResizeMode="CanResizeWithGrip">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
        <TextBlock Text="选择需要加载的 Sheet" Style="{StaticResource SectionTitleTextStyle}" />
        <TextBlock Grid.Row="1" Text="名称包含“透视”的 Sheet 默认不选；可以手动勾选。" Style="{StaticResource SecondaryTextStyle}" Margin="0,7,0,14" />
        <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="10">
            <ListBox ItemsSource="{Binding Choices}" BorderThickness="0" Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate><CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected}" Padding="8" /></DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="取消" Click="Cancel_OnClick" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" />
            <Button Content="加载选中 Sheet" Click="Confirm_OnClick" Style="{StaticResource PrimaryButtonStyle}" />
        </StackPanel>
    </Grid>
</Window>
