<ui:FluentWindow x:Class="SkillExam.App.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:controls="clr-namespace:SkillExam.App.Controls"
                 Title="技能士考试刷题系统 V3.1"
                 Icon="/Assets/AppIcon.ico"
                 Width="1100" Height="760" MinWidth="1000" MinHeight="700"
                 ExtendsContentIntoTitleBar="True"
                 WindowStartupLocation="CenterScreen" Background="{StaticResource PageBackgroundBrush}">
    <Grid Background="{StaticResource PageBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ui:TitleBar Title="技能士考试刷题系统 V3.1"
                     ShowMinimize="True" ShowMaximize="True" ShowClose="True"
                     Height="48" />
        <Grid Grid.Row="1">
            <ContentControl Content="{Binding CurrentPage}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" />
            <controls:LoadingOverlay IsActive="{Binding IsBusy}" Message="{Binding LoadingMessage}" Panel.ZIndex="100" />
        </Grid>
    </Grid>
</ui:FluentWindow>
